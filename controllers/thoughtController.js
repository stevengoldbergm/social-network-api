// Import models and the Reaction schema
const { User, Thought, Reaction } = require('../models');

// ---------- Export methods ---------- //
module.exports = {
    // ---------- Thought api calls ---------- //
    
}

